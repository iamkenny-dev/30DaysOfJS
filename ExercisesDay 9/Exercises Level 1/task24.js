// Use findIndex to find the position of the first country containing only six letters in the countries array

const check = countries.findIndex((country) => country.length === 6);
console.log(check);
