// Declare a function name capitalizeArray. It takes array as a parameter and it returns the - capitalizedarray.

function capitalizeArray(...arr) {
	    let capital = arr.toString().toUpperCase();
	    
	    console.log(capital.split(','))
}
capitalizeArray('r', 't');
